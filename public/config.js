window.APP_CONFIG = {
  // Set your deployed backend URL, example:
  // API_BASE_URL: "https://your-backend.up.railway.app"
  // Keep empty for local Docker (same origin).
  API_BASE_URL: "https://apilibretranslate.up.railway.app"
};
